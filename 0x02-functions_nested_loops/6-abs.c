#include "main.h"
/**
 * _abs - main function
 * File - 6-abs.c
 *
 * Author; Unekwu Shaibu
 *
 * Return: returns n* -one.
 *
 * @n: integer to make absolute.
*/
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}

